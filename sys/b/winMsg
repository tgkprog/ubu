#!/usr/bin/env bash
set -euo pipefail

sleep 4
SCRIPT_DIR="$(cd -- "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if [ "$#" -eq 0 ]; then
  set -- "dns & cpu throttle"
fi

 nohup python "$SCRIPT_DIR/winMsg.py" "$@" >/dev/null 2>&1 &
