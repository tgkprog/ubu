#!/bin/bash
if [ "$1" = "/?" ]; then echo "memtrack - Track system memory usage"; exit 0; fi
if [ "$1" = "/help" ]; then
  echo "memtrack - Memory usage tracker"
  echo ""
  echo "Usage: memtrack"
  echo ""
  echo "Tracks and monitors system memory usage over time."
  echo "Runs memTrack.py with sudo privileges."
  exit 0
fi

# Calls memTrack.py manually
sudo python3 /home/ubuntu/code/gt/tgk/ubu/sys/memTrack.py
